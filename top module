vga_sprite_v2(
		.clk_50MHz(CLOCK_50),
		.reset(1'b0),
		
		.move_right(KEY[0]),
		.move_left(KEY[2]),
		
		.red(VGA_R),
		.green(VGA_G),
		.blue(VGA_B),
		
		.hsync(VGA_HS),
		.vsync(VGA_VS),
		.sync(VGA_SYNC_N),
		
		.clk(VGA_CLK),
		.blank(VGA_BLANK_N)
);
